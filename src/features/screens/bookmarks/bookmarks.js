import * as React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

export default function BookmarksDetail({ navigation }) {
  return (
    <SafeAreaView>
      <View>
        <Text>BookmarksDetail</Text>
      </View>
    </SafeAreaView>
  );
}
